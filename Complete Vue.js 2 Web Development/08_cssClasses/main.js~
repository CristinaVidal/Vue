const app = new Vue({
	el: '#app',
	data: {
		checkBorder: false,
		checkBackground: false,
		checkRadius: false
	},
	computed: {
		isAllSelected() {
			return this.checkBorder &&
				this.checkBackground &&
				this.checkRadius;
		}
	}
});
