<!DOCTYPE html>
<html>
	<head>
		<meta chatset="utf-8">
		<title>Vue.js test</title>
		<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
		<link rel="stylesheet" href="style.css">
	</head>
	<body>
		<div id="app">
			<div v-if="visible">Visible !</div>
			<div v-else>'Visible !' no se ve, así que aquí estoy :)</div>
			<div v-if="!visible">Invisible !</div>
			<div v-else>'Invisible !' no se ve, así que aquí estoy :)</div>

			<div> Tienes {{ edad }} años, por lo que: </div>
			<ul>
				<li v-if="edad < 18"> 
					No puedes hacer nada jaja
				</li>
				<li v-else-if="edad >= 18 && edad < 21">
					En España puedes beber, en EEUU igual no jaja
				</li>
				<li v-else>
					Vuela como un pajarillo hacia la cola del paro
				</li>
			</ul>

			<div> Probando v-for... Probando, 1, 2 </div>
			<template v-if="people && people.length">
				<ul>
					<li v-for="person in people">
						{{ person.name }}: {{ getStatus(person.age) }}
					</li>
				<ul>
			</template>
		</div>

		<script src="https://unpkg.com/vue"></script>
		<script src="main.js"></script>
	</body>
</html>
